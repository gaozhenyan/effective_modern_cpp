#include "article03.h"

#include <string>
#include <iostream>

int main() {
    const int i = 0;
    decltype(i) ti = 0;
    bool f(const std::string &s);
    struct Point {
        int x, y;
    };

    std::string s;

    return 0;
}